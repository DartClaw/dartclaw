# 0.26 gap and code review

## Executive Summary

**Final overall readiness: Ready.** Gap: PASS; code, security and documents: Ready. Seven medium findings were fixed; independent closure review found no remaining notable findings. One initial candidate was withdrawn after falsification. Initial gap readiness was FAIL and code readiness was Needs Fixes; no high or critical finding survived discovery.

Scope: released 0.25.2 (`34856eac`) through `b919e1e9`, all 17 Phase A stories and nine Phase B stories. The private Phase A PRD/plan/FIS and public Phase B bundle govern intent; `search-contract-correction.md` supersedes generative answer-sufficiency acceptance. Review lenses: gap, code, security, adversarial, followed by a fresh critic and independent findings filter.

## Verdict

| Dimension     | Score | Threshold | Status |
|---------------|-------|-----------|--------|
| Functionality | 10/10 | >= 7      | PASS |
| Completeness  | 10/10 | >= 9      | PASS |
| Wiring        | 10/10 | >= 8      | PASS |

**Overall: PASS**

## Coverage and verification

Database contracts, schemas, repositories, ownership and async lifecycles; FTS, conversations, providers, vectors, fusion, synchronization, inspection and provenance; workflow DSL/schema, native packaging, evaluation and release evidence were reviewed in separate partitions. The coordinator checked cross-partition wiring and deletion completeness; a fresh critic checked lifecycle failures and boundary assumptions. The partition reports below retain clause-level coverage, falsifiers, evidence and limitations. Guardrail passes checked 63 rules in total across five passes (overlapping rules are counted per pass).

Fresh baseline gate: 12/12 checks succeeded, including format, fatal-info analysis, workspace tests (11,931 passed, 44 configured/platform skips), live PostgreSQL contracts and hybrid wiring, architecture/fitness, both AOT builds and diff validation. Receipt: `.agent_temp/0.26-search-remediation/review-20260910-baseline/receipt.json`. Desktop/mobile task and workflow rendering, approval completion and HTMX navigation were visually checked; evidence: `.agent_temp/reviews/0.26/visual-verification.md`.

External native platforms, unseen held-out retrieval scoring and external CI/release qualification remain pending as already documented. Exposed regression evidence does not establish unseen acceptance. These are release qualifications, not newly discovered code defects.

## Accepted finding adjudication

The following filter is the authoritative disposition; earlier partition routing suggestions are superseded. Each accepted finding was primary, confidence 100, Class `code-defect`, Routing `Fix`; final closure status is below. R-01 preserves lexical fallback and empty-query no-work behavior while representing unknown counts as null. DB-02 preserves established transaction results through best-effort statement disposal. F01 checks evidence shape only; existing success policy remains authoritative.

# 0.26 findings filter

**Target:** the eight findings in `database-findings.md`, `retrieval-findings.md`, `workflow-release-findings.md`, and `critic-findings.md`
**Implementation range:** `34856eac..b919e1e9` (current `HEAD` is `b919e1e9`)
**Filter result:** 7 validated, 0 downgraded, 1 withdrawn. All seven validated findings route to `Fix`.

This pass checked only the supplied findings. Evidence covered the current implementations and immediate consumers, the released-0.25 baseline where DB-03 depends on preserved aggregate behavior, the pinned `postgres` 3.5.12 disposal implementation, root and package rules, and the governing Phase A/Phase B FIS clauses cited below.

| Finding | Verdict | Final severity / confidence | Evidence and filter reason | Proposed Class / Routing | Required change |
|---|---|---|---|---|---|
| R-01 | **VALIDATED** | MEDIUM / 100 | `_CurrentCorpusInventory.diagnosticMissingCount` returns `0` after both authenticated enumeration and lexical count fail (`current_corpus_inventory.dart:74-82`), so an unknown count is reported as a computed zero. The blank-query path also constructs the default zero without inventory work (`hybrid_search.dart:44-46`). S04 S02 explicitly requires an empty query to perform no constituent work, while S06 S03 requires a query unable to supply complete diagnostics to return 503. `HybridSearchBackend` internally requests diagnostics to preserve lexical fallback degradations, so throwing away the whole diagnostic would regress valid fallback results. | `code-defect` / **Fix** – the false sentinel is primary, certain, and the combined S04/S06 constraints determine a bounded correction. | Make only `SearchDiagnostics.unembeddedCount` nullable, default it to `null`, and return `null` when the diagnostic count cannot be computed. Preserve blank-query no-work behavior with a null count and preserve fallback hits/degradations. Have inspection return its existing 503 when the captured count is null, so every successful HTTP diagnostic still contains an integer. Record the minimal pinned-contract reconciliation in the FIS observation; do not coerce unknown to zero. |
| R-02 | **VALIDATED** | MEDIUM / 100 | The exported kernel predicates at `search_config.dart:33-46` own endpoint and credential transport policy. `http_embedding_provider.dart:158-169,189-195` repeats scheme, host, userinfo, query, fragment, HTTPS, and loopback decisions, while runtime already calls the kernel owner at `embedding_configuration.dart:52-55`. Both implementations are new in the reviewed range. This is the project’s explicit second-implementation defect class at a credential transport boundary. | `code-defect` / **Fix** – reuse is primary, mechanical, and preserves both validation stages. | Keep validation at configuration and provider construction, but make both call the kernel predicates. Retain provider-local normalization, non-empty API-key validation, and provider-specific error construction. |
| R-03 | **VALIDATED** | MEDIUM / 100 | `packages/dartclaw_search/` is a new production workspace package in the reviewed range and currently has neither `CLAUDE.md` nor the required `AGENTS.md` symlink. Root `AGENTS.md` states that every `packages/<name>/` has this pair and that package-local facts must stay current. The package’s existing public contracts, corpus ownership, and provider lifecycle supply factual invariants; authoring them requires no unresolved product choice. | `code-defect` / **Fix** – the mandatory convention, target, and symlink shape are explicit and bounded. | Add a concise package-local `CLAUDE.md` containing only implemented package-specific boundaries and invariants, then add the standard `AGENTS.md` symlink to it. Verify the link and repository symlink/fitness gate. |
| DB-01 | **VALIDATED** | MEDIUM / 100 | The startup catch releases the interlock before closing `_taskBackend` (`storage_wiring.dart:226-238`), while `closeBackends` correctly calls `beginShutdown`, closes the task backend, then releases (`:709-735`). S10 OC01 and TI03 explicitly require ownership release after pool close. The catch can run after backend/interlock acquisition when later preparation or repository assembly fails. | `code-defect` / **Fix** – the required order and existing failure-aggregating helper make the correction mechanical. | Reuse `closeBackends()` on this startup-failure path, catch/log any cleanup failure so the original startup exception remains the reported cause, then dispose turn state and follow the existing fatal path. Add an observable close-before-release regression, including a close failure that still releases ownership without replacing the startup error. |
| DB-02 | **VALIDATED** | MEDIUM / 100 | On body failure, `PostgresBackend.transaction` awaits `_deactivate()` before rethrowing the body error (`postgres_backend.dart:168-175`); after `COMMIT`, and also when `COMMIT` throws, it awaits the same cleanup in `finally` (`:176-181`). `_deactivate` stops at the first statement disposal failure (`:361-365`). Pinned `postgres` 3.5.12 `_PreparedStatement.dispose` only skips when the connection is closing and otherwise awaits portal and statement close protocol messages (`v3/connection.dart:764-770`), so disposal can fail. The outer dispatch policy converts an unpassed post-dispatch error to `StorageUnknownOutcomeException`, so the original report’s raw-driver-leak subclaim is falsified, but cleanup can still replace the established body error, commit failure, or committed body return. S01 Technical Overview item 3 explicitly requires `transaction()` to return the body value and rethrow the body error after rollback; S07 OC03 and scenario S03 consume that contract unchanged. Those outcomes leave no product decision open for statement cleanup. | `code-defect` / **Fix** – the primary defect and precedence are explicit, and best-effort disposal through the existing logger is bounded with no API or exception change. | Mark every transaction statement inactive, attempt every close best-effort, and log a generic redacted cleanup warning through the existing `PostgresBackend._log`. Cleanup must not replace an established body error, commit failure, or committed body value. Add disposal-failure tests for body-error/rollback, commit failure, and successful-commit paths, including multiple statements so all closes are attempted. |
| DB-03 | **WITHDRAWN** | – / 100 | **Concrete falsifier:** the governing S04 FIS pins zero user-visible change and requires `SqliteTaskRepository` insert/update aggregate behavior to equal released 0.25; S04 TI04 explicitly retains and awaits `_upsertAgentExecution`. The baseline at `34856eac` already calls `_upsertAgentExecution` from task insert/update/mutable update. Separately, S05 S03 explicitly defines the production-shaped atomic action as involving both the task repository and agent-execution repository, and requires neither task nor execution to survive a later participant failure. These upstream clauses establish two distinct operations: aggregate persistence by the task repository and lifecycle/event/not-found semantics by the dedicated repository. Different create/update behavior therefore does not prove a duplicated policy owner, and removing either path would contradict the parity/atomicity baseline. | **Withdrawn** – no Class or Routing applies. | None. Do not change the aggregate boundary without a new requirement that supersedes S04/S05 parity. |
| C-01 | **VALIDATED** | MEDIUM / 100 | `DartclawRuntime.shutdown()` promises that execution prepare-shutdown or server shutdown failures propagate (`service_wiring.dart:400-405`), but awaits `closeStorage()` in `finally` (`:406-415`). If an earlier shutdown step and `StorageWiring.closeBackends()` both throw, Dart’s `finally` semantics replace the in-flight primary exception and stack with the cleanup failure. `closeBackends()` intentionally throws its first collected teardown failure after attempting the remaining resources (`storage_wiring.dart:709-735`), so this is an executable compound-failure path introduced when the reviewed diff replaced synchronous `searchDb?.close()` with awaited `closeStorage()`. | `code-defect` / **Fix** – the public method contract explicitly fixes precedence, and the existing logger provides a bounded compound-failure path. | Capture the primary shutdown error and stack, always attempt `closeStorage()`, and rethrow the primary with `Error.throwWithStackTrace` when both fail. Log the storage cleanup failure generically through the existing runtime logger in that compound case; propagate it when it is the only failure. Test primary-plus-cleanup failure identity/stack, cleanup-only propagation, and that storage close is attempted in both cases. |
| F01 | **VALIDATED** | MEDIUM / 100 | `validateNativeEmbeddingEvidence` checks top-level presence, two-list/four-list cardinality, operation names, and Linux OpenMP only (`native_embedding_platform_gate.dart:357-400`). It does not validate the required nested archive/native/model/metric shapes, typed process fields, or `outputStreamsClosed`. The focused test accepts empty archive maps, partial native/model/metric maps, and failure records missing that field after the OpenMP flag is changed (`native_embedding_platform_gate_test.dart:164-199`). S07’s pinned evidence interface lists these fields and TI03 says malformed evidence and all required schema fields are proof obligations. | `code-defect` / **Fix** – shape completeness is primary, deterministic, and bounded by the existing evidence object. | Extend this single validator to check required nested keys, collection cardinality, and field types, including `outputStreamsClosed`. Preserve the existing `passed` timing and per-operation success calculation as the sole authority; do not re-evaluate thresholds, hashes, or success policy in the shape validator and do not add another schema/policy layer. Replace the permissive fixture with one complete valid record plus missing/wrong-type mutations. |

Filter summary: 7 validated, 0 downgraded, 1 withdrawn.

## Finding impacts and verification

All locations and evidence above refer to the reviewed baseline. Reviewer seats were database gap/code/security (DB), retrieval gap/code/security (R), workflow/release (F01), and fresh critic (C01).

| Finding | Threatened invariant and impact | Closure proof required |
|---|---|---|
| R-01 | Unknown inventory must not appear healthy; optional diagnostics must preserve retrieval results. | Count failures and blank queries retain null; fallback hits/degradations survive; inspection rejects unavailable counts. |
| R-02 | Credential transport policy has one owner; duplicate validators can drift. | Kernel/provider endpoint and credential boundary parity. |
| R-03 | Every production package carries its implemented local boundary rules. | Factual package guidance, valid symlink and architecture/fitness gate. |
| DB-01 | Serving ownership outlives the task pool; cleanup cannot hide startup failure. | Observable pool-close-before-release order, including compound failure. |
| DB-02 | Statement disposal cannot replace a transaction's established outcome or stop remaining disposal. | Multiple disposal failures after body failure, commit failure and successful commit preserve each outcome. |
| C-01 | Shutdown preserves its primary exception and stack while attempting storage closure. | Compound failure identity/stack and cleanup-only propagation. |
| F01 | Retained native evidence contains all required facts; shape validation does not duplicate success policy. | Complete valid and failed records accepted; missing/wrong-type nested fields rejected; native packaging contracts. |

## Clause coverage

Coverage tables retain the discovery pass evidence. DB-03 references in that pass were subsequently falsified by the authoritative adjudication above and carry no remediation authority.

### database

| Intent slice | Outcomes, scenarios, structural/work areas mapped | Actual evidence and positive proof | Falsifier attempted and result |
|---|---|---|---|
| S01 backend seam and SQLite | OC01–OC03; acceptance S01–S07; SC01–SC04; TI01–TI08 | `DatabaseBackend`, `SqliteBackend`, placeholder rewriter, goal repository, and backend contract tests. The interface documents transaction ownership and original-error preservation. | Traced literal/comment placeholders, concurrent queue ownership, owner re-entry, nested calls, statement expiry, rollback, and close idempotence. SQLite focused tests passed; cleanup preservation remains relevant to the PostgreSQL implementation in DB-02. |
| S02 SQLite schema gate | OC01–OC03; acceptance S01–S07; SC01–SC07; TI01–TI07 | `schema_identity.dart`, `sqlite_schema_gate.dart`, released fixtures, adoption and rollback tests. The same prepared backend is passed from the gate to repositories. | Tried fresh-bootstrap partial DDL, malformed/missing marker, released authoritative adoption, incompatible authoritative store, orphan column, incomplete rebuild source, and transition retry. Focused rollback/adoption tests passed and no second schema authority was found. |
| S04 task/event/trace/KG async conversion | OC01–OC03; acceptance S01–S06; SC01–SC05; TI01–TI09 | Seam-backed task/event/trace/KG implementations and current runtime/web/MCP callers were inspected; storage wiring prepares the backend before construction. | Searched storage-touching calls for lost awaits and checked event recorder sequencing. No lost durability await survived; the task aggregate still contains a second agent-execution write path (DB-03). |
| S05 execution/workflow storage | OC01–OC03; acceptance S01–S06; SC01–SC05; TI01–TI07 | Agent/step/run repositories share the prepared backend and `SqliteExecutionRepositoryTransactor`; task service and workflow task factory transaction call sites were traced. | Attacked partial task/agent/step/run writes, outside-operation ordering, nested action rejection, and cleanup gate-first behavior. Atomic paths are present; DB-03 records the redundant write authority. |
| S06 composition and census | OC01–OC04; acceptance S01–S07; SC01–SC08; TI01–TI11 | `StorageWiring`, CLI consumers, barrels, SQLite importer fitness allowlist, and teardown paths were inspected. Raw task/search DB helpers and old factory typedefs are absent. | Traced supported/incompatible starts, rebuild refusal, CLI ownership, partial construction, and repeated close. Normal shutdown closes the task pool before the interlock; the active-store failure catch reverses that order (DB-01). |
| S07 PostgreSQL backend/schema | OC01–OC05; acceptance S01–S07; SC01–SC07; TI01–TI08 | `PostgresBackend`, dispatch policy, placeholder rewriting, schema gate, typed storage exceptions, live test sources, and PostgreSQL 14 CI job were inspected. | Attacked unknown dispatch/replay, transaction ownership, raw driver leakage, schema exactness, bootstrap rollback, unrelated objects, and pool lifecycle. DB-02 survives. The FIS acceptance scenario treats a non-empty markerless database as incompatible, while Technical Overview item 10 defines emptiness by absence of DartClaw-required relations; implementation and tests follow the acceptance scenario, so the prose should be reconciled before it is reused as a baseline. |
| S08 credentials, TLS, redaction | OC01–OC04; acceptance S01–S07; SC01–SC06; TI01–TI09 | Database config parser/validator, credential registry resolution, `PostgresConnectionPosture`, config serializer, message redactor, audit hooks, and tests were inspected. | Tried persisted literal credentials, unresolved references, non-loopback `sslmode=disable`, loopback development posture, URL/userinfo rendering, and superuser warnings. All reviewed boundaries fail closed or redact as specified. |
| S10 serving interlock and switching | OC01–OC05; acceptance S01–S08; SC01–SC08; TI01–TI08 | Current `postgres_interlock.dart` lines 302–316 were reviewed directly, along with quarantine recovery, orphan probe, backend selection, storage wiring, and interlock tests. | Checked lock loss during recovery, callback scheduling, recovery cancellation, shutdown ordering, inactive-store scanning, backend switching, and PostgreSQL SQLite-free paths. The candidate callback race was falsified because no `await` occurs in the synchronous candidate-adoption block. DB-01 remains on the separate startup-failure path. |
| S11 dual contracts and CI | OC01–OC03; acceptance S01–S08; SC01–SC06; TI01–TI10 | Contract manifest/groups, backend/repository/schema harnesses, PostgreSQL live entry, proof script, CI service job, and release gate were inspected. Coordinator reports dependency, format, analyze, workspace, PostgreSQL contract, hybrid, architecture, fitness, and AOT gates green. | Looked for skipped live infrastructure, threshold/count proof, stale whitelist escape, missing repository family, and release-job drift. No survivor. |
| S12 operator/developer docs | OC01–OC04; acceptance S01–S07; SC01–SC07; TI01–TI06 | PostgreSQL, configuration, deployment, security, architecture, and storage model documentation in the assigned file list was compared with the shipped config and lifecycle seams. | Searched for resolvable DSN passwords, retired package/store names, implicit transfer claims, missing reset/reindex guidance, and generated-reference drift. No code/security survivor; the S07 FIS wording conflict above is the only intent-document issue retained. |
| S14 authoritative rename/adoption | OC01–OC04; acceptance S01–S08; SC01–SC06; TI01–TI06 | `dartclawDbPath`, adoption/checkpoint/rename module, presence probe, wiring/CLI call sites, docs, and focused adoption tests. | Tried both-files ambiguity, WAL-resident data, no sidecars, failed checkpoint/rename, diagnostics, and read-only presence checks. Focused adoption tests passed; no second adoption path found. |
| S15 filesystem local state | OC01–OC03; acceptance S01–S07; SC01–SC07; TI01–TI07 | `TurnStateStore`, `WebhookDeliveryStore`, construction paths, crash-test sources, path hashing, exclusive marker creation, atomic file helper use, and focused store tests. | Tried corrupt/torn state, bodyless markers, stale reclaim/release, hostile and case-variant IDs, temp-file sweep, and mutation return timing. Mutations are synchronous through durable rename and tests passed; no database file is opened by either store. |

Focused command: `dart test --reporter=failures-only` over SQLite backend, rollback gate, authoritative adoption, turn state, webhook delivery, interlock, and three storage-wiring suites: **+68, all passed**. The coordinator supplied the green root-gate result above; this partition did not duplicate the full suite.

### retrieval

| Intent / work area | Evidence inspected | Concrete falsifiers applied | Result |
|---|---|---|---|
| Phase A S03 OC01-04, S01-09, SC01-06 – FTS contract, SQLite identity, memory projection/composition | Private S03; `vector_index.dart`, `sqlite_fts_index.dart`, `memory_index_projection.dart`, `composed_search_backend.dart`; associated tests | Same ID under another owner; malformed canonical metadata; FTS metacharacters; empty query; partial mutation; wrong error propagation; rebuild divergence | Covered; no finding |
| Phase A S09 OC01-04, S01-08, SC01-06 – PostgreSQL FTS/KG and runtime selection | Private S09; `postgres_fts_index.dart`, PostgreSQL search/wiring tests; coordinator's live PostgreSQL proof | Owner crossover; SQL/query-language injection; wrong morphology; unqualified extension operator; accidental SQLite fallback | Covered; no finding |
| Phase A S13 OC01-04, S01-09, SC01-07 – conversation projection/indexer/service lifecycle | Private S13; `conversation_index_projection.dart`, `conversation_indexer.dart`, `conversation_search_service.dart`, runtime storage wiring and tests | Append/clear/delete/type transition; before-delete callback timing; observer failure; archive/resume; memory/conversation corpus collision | Covered; the pre-delete lexical enumeration matches the recorded async-lifecycle observation |
| Phase B PRD FR1/FR3; S01 OC01-04/S01-06/SC01-05; S04 OC01-03/S01-06/SC01-04 – contracts, canonical chunks, hybrid fusion, synchronization | PRD/plan/S01/S04; kernel vector contract; `current_corpus_inventory.dart`, `hybrid_search.dart`, `vector_synchronizer.dart`, adapter and focused tests | Wrong owner/corpus/model/dimension/hash; stale/deleted source; delayed source mutation; empty corpus/query; provider/vector failure; rank cutoff/ties; incomplete current-corpus enumeration | **Finding R-01**; remaining clauses covered |
| Phase B S02 OC01-04/S01-08/SC01-05 – SQLite/PostgreSQL vector stores | S02; vector schemas/indexes, factories, migrations, unit/live tests | Non-finite vector; dimension/fingerprint mismatch; cross-owner/corpus hit; partial schema; rollback failure; unsafe SQL interpolation; unqualified PostgreSQL vector operations | Covered; no finding |
| Phase B S03 OC01-04/S01-06/SC01-05 – native/HTTP providers and model acquisition | S03; `embedding_providers.dart`, native/HTTP provider, model acquirer, runtime configuration and tests | Unsafe endpoint/userinfo/query/fragment; credential over non-loopback HTTP; redirect escape/loop; malformed payload/cardinality/dimension; secret-bearing error; timeout/dispose; partial model file | **Finding R-02**; behavior otherwise covered. Manual redirect handling matches the accepted FIS observation |
| Phase B S05 OC01-04/S01-08/SC01-04 – two-corpus runtime activation, guarded queries, counts and shutdown | S05; runtime `storage_wiring.dart`, embedding configuration, memory status, rebuild CLI and lifecycle tests; coordinator's queue-drain/close-order trace | FTS/QMD constructs provider; wrong setup order; shared corpus identity; mutation lost before close; query released across revision change; vector backend/provider closed before queue/indexer drain | Covered; no finding |
| Phase B S06 OC01-03/S01-06/SC01-03 – authenticated inspection and turn provenance | S06; inspection route/server composition/CLI, turn guard, `ToolCallRecord`, API/guard tests | Unauthenticated route; unknown fields/body/limit; unavailable diagnostics leaking hits; forged/unmatched/failed tool output; malformed locator JSON; duplicate or >50 locators; text/vector/secret disclosure | Covered; no finding |
| Package and architecture guardrails | Workspace/package manifests and barrels; root/package rules; ADR-050/054; development architecture guidelines | Reverse dependency; `lib/src` cross-package import; implicit barrel export; duplicate concern owner; absent package-local rules | **Findings R-02 and R-03**; dependency tier and exports covered |
| Refactor invariants triggered by new package, parameter threading, lifecycle relocation, caches/native artifacts | Diff inventory; construction and disposal call sites; owner/model/dimension/corpus searches; coordinator's removal census | Dropped `userId`, fingerprint or dimension; old factory seam still referenced; double/early close; stale cache after mutation; generated/native artifact mismatch | Covered for retrieval-owned code. Native packaging artifacts and evaluator outputs remain assigned to S07-S09 reviewer |
| Correction boundary | `search-contract-correction.md`, PRD and hybrid tests | Host-side answer-sufficiency heuristic; semantic retrieval unable to add topical context beyond lexical matches | Covered; no answer-sufficiency finding raised |

### workflow-release

| Assigned intent | Outcomes / scenarios / criteria covered | Work areas inspected | Evidence and falsifiers checked | Result |
|---|---|---|---|---|
| Phase A S16 declarative workflow rules | OC01–OC03; S01–S06; SC01–SC08; TI01–TI06 | `workflow_dsl_rules.dart`, parser, validator and three validation rule files, definition model, workflow rule/golden/corpus tests, package architecture notes | Enumerated block/key/alias/value-kind parity; fresh generic field declaration; wrong type, unknown key/type, applicability, mutual exclusion, required field, null, snake/camel aliases; byte-stable diagnostic order/text; seven maintained workflows; no framework/barrel leakage. The typed-field-set candidate was falsified against TI03's explicit “parse arms, coercions and normalization are untouched” boundary. | Covered, no finding |
| Phase A S17 published workflow schema | OC01–OC04; S01–S06; SC01–SC09; TI01–TI07 | emitter, generator/check mode, committed schema, JSON-schema support and corpus suites, fitness runner/baseline, workflow architecture, contributor/user docs, changelog | Closed objects; conditional per-type applicability; parser polymorphisms (`prompt`, `outputs`, `worktree`, `timeout`, schema presets, aliases); malformed keys/types/enums; artifact↔rule-source bidirectional enumeration; deterministic regeneration and drift message; no `$id`/timestamp/template/manual writer; fitness integration and docs discoverability. | Covered, no finding |
| Phase B S07 native packaging/platform proof | OC01–OC03; S01–S06; SC01–SC04; TI01–TI04 | native manifest, preparation/model tools, workspace staging, POSIX/Windows builds, release workflow, AOT probe, outer process observer, packaging/platform/release tests and retained macOS evidence | Exact size/SHA before atomic cache/stage publication; redirect cap; offline miss/no upstream fallback; two same-flavor archives and library inventories; selected loader; success metrics; absent/corrupt model, missing/invalid loader cases; SIGTERM→SIGKILL, retained pipes, output closure, sanitized errors; Linux OpenMP field; platform-matrix wiring. Malformed evidence records expose F01. | Covered; F01 |
| Phase B S08 sealed retrieval evaluator | OC01–OC04; S01–S07; SC01–SC05; TI01–TI03 | evaluator rules, pipeline, CLI, focused tests/support, frozen hashes/assets, production SQLite/PostgreSQL/vector/fusion seams, protocol-2 correction and retained reports | Pre-score asset hash/shape checks; unknown IDs, malformed relevance/no-match labels, missing booleans; exact match/no-match and owner isolation; 2×3×2×3 matrix and per-language query partitions over the full multilingual corpus; Recall/MRR/nDCG arithmetic and tolerance-boundary mutants; warm-only latency/provenance; production pipeline use; exact hybrid wiki composition. A corpus language prefilter was rejected as a candidate because the FIS explicitly requires reprojecting the full corpus for each language slice. | Covered, no finding |
| Phase B S09 operations/release preparation | OC01–OC04; S01–S06; SC01–SC05; TI01–TI04 | PRD/plan, operator and architecture docs, versions/manifests, release-preparation guidance, story receipts, final combined manifest, retrieval decision/correction and committed-source binding | Four embedding config keys; local/HTTP trust-boundary docs; corpus controls and diagnostics; architecture/ADR/glossary consistency; version pins; pending-before-run manifest ownership; no DSN/API key/query/vector payload; protocol-2 exposed-regression rows/gates and historical record preservation; source hashes and clean committed binding. | Covered, no finding |
| Security and scope | All assigned surfaces | download/cache/stage boundaries, archive extraction/build inputs, evidence serialization, evaluation owner partitioning, public docs and release records | No consumption before checksum; no network fallback offline; controlled redirect count; no absolute paths/credentials/raw query or vectors in evidence; owner-isolation gates are non-aggregate; generated schema introduces no runtime validation or new API; release artifacts do not claim unseen acceptance. | Covered, Ready |

## Cross-boundary and critic coverage

The coordinator traced old factory removals, async storage consumers, queue/indexer drain before provider/vector closure, HTTP authentication and inspection shape, exact tool-policy enforcement, and resource ownership across construction failure. Eight applicable guardrails were checked without an additional finding. The fresh critic checked eleven guardrails and found C-01; its remaining retrieval hypotheses matched R-01/R-02/R-03.

Falsified candidates: concurrent embedding dimension assignment and interlock adoption contain no intervening await; workflow typed-field bookkeeping preserves the explicitly retained parser arms; language slices intentionally use the full multilingual evaluation corpus. Frozen failure evidence remains historical. The workflow sidebar listener ordering issue is pre-existing and explicitly outside the changed listener scope, so it is not a 0.26 regression finding.

## Verification limits

The 44 baseline skips include configured/platform tests; they are not counted as passes. Linux/Windows native execution, unseen retrieval qualification and external release CI remain pending. No independent third-party JSON Schema validator was run; schema drift, malformed corpus and fitness tests cover the emitted vocabulary. Focused reviewer source/test inspections supplement, rather than replace, the coordinator's real workspace and PostgreSQL execution.

## Closure review and final verification

A fresh reviewer checked all seven finding families, touched code/tests/docs, and causal regressions. It confirmed requirement intent, failure precedence, unknown-vs-zero semantics, endpoint policy reuse, evidence completeness without a second success policy, and the documented package boundary. No notable finding survived. Eleven guardrails were checked with zero findings. The closure verdict above applies to the reviewed implementation and remediation; external release qualifications remain as stated in Verification limits. Detailed closure evidence: `.agent_temp/reviews/0.26/remediation-closure.md`.

Final receipt: `.agent_temp/0.26-search-remediation/review-20260910-remediation-final/receipt.json`. All 12 checks passed and `sourcePreserved` is true: dependency lock enforcement for the workspace and mascot tool, asset generation, whole-tree formatting, fatal-info analysis, workspace tests, live PostgreSQL contracts, live PostgreSQL hybrid wiring, architecture, fitness, both AOT binaries, and diff validation. Workspace totals: **11,943 passed, 44 configured/platform skips**. The temporary database was removed and the owned test container restored to stopped.

An earlier remediation gate stopped at analysis because a new runtime test directly imported `postgres`; declaring the existing `^3.5.12` version as a dev dependency resolved it before the complete rerun. The architecture precheck required a downward ratchet after endpoint-policy reuse: search measured 1,490 lines, ceiling reduced from 1,994 to 1,986. No ceiling was raised.

Real native evidence: `.agent_temp/reviews/0.26/native-macos-arm64-remediation.json`. The macOS ARM64 gate passed: cold embed 2,408 ms, warm query 9 ms, document batch 12 ms, disposal 10 ms, dimension 768, document cardinality 2. Both release archives recorded; all four failure cases observed natural process exit and closed streams with no forced termination. These are working-tree remediation receipts, not new committed release qualification.

## Remediation Status

- **R-01 – truthful diagnostic counts** – RESOLVED – null distinguishes unknown from measured zero; empty-query no-work, fallback results/degradations and inspection 503 are tested; closure review passes.
- **R-02 – one endpoint policy owner** – RESOLVED – provider calls kernel predicates; boundary tests and closure review pass.
- **R-03 – search package guidance** – RESOLVED – factual package rules and valid relative symlink added; fitness and closure review pass.
- **DB-01 – startup ownership order** – RESOLVED – existing closeBackends owner closes the pool before releasing ownership, including close failure; original startup error remains reported; regression and closure review pass.
- **DB-02 – transaction outcome preservation** – RESOLVED – existing statement close helper is attempted best-effort for every statement; body/commit error identity and committed value survive; regression and closure review pass.
- **DB-03 – aggregate persistence candidate** – SURFACED – withdrawn because upstream parity/atomicity contracts falsified the claim; no Class/Routing or edit authority applies.
- **C-01 – primary shutdown failure preservation** – RESOLVED – primary error/stack retained, storage cleanup always attempted, secondary warning contains no raw exception; regression and closure review pass.
- **F01 – complete native evidence shape** – RESOLVED – required nested fields/types/cardinality validated in the existing owner; passing and failing evidence supported without duplicating success policy; focused tests, real native gate and closure review pass.
