# Wireframe-to-Implementation Deviations

Intentional divergences between wireframes and shipped code. Each entry documents the deviation, resolution, and which artifact is canonical.

| # | Area | Wireframe Shows | Implementation Does | Resolution | Canonical |
|---|------|----------------|---------------------|------------|-----------|
| 1 | session-info | Slide-in panel overlay accessible from topbar | Standalone page at `/sessions/<id>/info` | Page layout wins -- better for mobile, simpler routing | Implementation |
| 2 | topbar | Single generic topbar component | 3 variants: `sessionTopbar` (info + reset), `plainTopbar`, `pageTopbar` (back link) | Wireframes updated on 2026-03-09 to use variant-specific topbars where relevant | Implementation |
| 3 | SPA navigation | Static page links (`<a href>`) | Full SPA via HTMX (`hx-get`, `hx-target="#main-content"`, `hx-push-url`) | Full SPA wins -- wireframes remain static mockups, but route/page splits now match live implementation | Implementation |
| 4 | guard blocks | Multiple guard types with rich detail (file, command, network, content) | Subset of guard types currently implemented | Wireframe is design intent for future guards -- annotate which are live vs planned | Wireframe |
| 5 | token reveal | Reveal/copy buttons on settings page | CLI-only via `dartclaw token show` | Update wireframe to match CLI-only approach -- no browser token reveal for security | Implementation |
| 6 | chat sidebar typing | Single generic session list | Typed sections: `Workspace` (label) / `Agent` (item), `Channels`, `Sessions` (`cron` hidden) | Renamed `Main`→`Workspace`, `Main` item→`Agent` across all chat wireframes on 2026-03-06 for clarity | Implementation |
| 7 | system/admin sidebar | ~~Condensed sidebar~~ → Unified sidebar | All pages (including system/admin) now use the same unified sidebar with sessions, channels, workspace + SYSTEM nav. No active session highlighted on system pages. Condensed sidebar removed. | Unified sidebar provides consistent navigation and eliminates empty-space UX issue. Reverted 2026-03-06. | Both (aligned) |
| 8 | channel pairing vs policy | Pairing pages previously mixed connection setup with DM policy UI | Access policy now lives on dedicated channel detail pages at `/settings/channels/whatsapp` and `/settings/channels/signal`; pairing pages are connection/setup surfaces only | Wireframes updated on 2026-03-09 to reflect the split cleanly | Implementation |
| 9 | signal pairing | Signal wireframe previously diverged from WhatsApp structure and under-emphasized QR/device-link flow | Implementation is QR-first for device linking, with hidden/deferred SMS/captcha states | Signal wireframe updated on 2026-03-09 to mirror WhatsApp structure and mark deferred states explicitly | Implementation |
| 10 | reflection session | Dedicated cron/reflection session UI | Generic session page still renders cron sessions with no cron-specific treatment | Keep wireframe as future feature until cron-specific UI exists | Wireframe |
| 11 | channel configure routing | Static wireframes imply "Configure" always lands on the same surface | Implementation now keeps channel detail routes as the canonical configure surface; when a channel is enabled but not paired, the detail page renders a pairing-needed/setup state instead of redirecting away | Settings and channel-detail wireframes should treat `/settings/channels/<type>` as canonical, with pairing/setup states rendered inline when needed | Implementation |
| 12 | workflow detail actions | Resume/Cancel controls represent every non-terminal hold the same way | Workflow detail now distinguishes `Paused`, `Awaiting approval`, and `Failed`; failed runs render a dedicated Retry button, approval-style holds use Approve/Reject, and deliberate pauses keep Resume | Update the workflow-detail wireframe to treat Retry as failed-only and to label approval-style holds separately from operator pauses | Implementation |
| 13 | 0.19 knowledge UI style | `knowledge-hub`, `kg-timeline`, `source-attribution` are deliberately **low-fi grayscale and design-system-agnostic** (self-contained inline CSS, no `tokens.css`/`components.css` link), unlike the earlier high-fidelity wireframes that build on the current design system | The 0.19 knowledge UI must be built on the new **Afterglow** design system at implementation time (ROADMAP directive: "not migrated after"); visual styling is intentionally deferred | These three capture structure/IA/states only; apply Afterglow visuals at implementation, then re-validate. Do not treat their grayscale look as the target visual design | Wireframe (structure only) |
| 14 | login identity | Standard centered authentication card with text wordmark | CRT terminal-frame login hero with a pixel-art mascot and favicon | Intentional single CRT surface; the mascot is only rendered at its legible pixel-art size. Recorded 2026-07-21. | Implementation |
| 15 | empty states | Generic emoji/mascot empty-state treatments | Tasks and Projects use the CSS-only claw-mark; task detail stays mark-free, and app/chat empty states retain the `❯_` prompt glyph | Intentional scarcity rule: one claw moment per view; no full mascot is used in an empty state. Recorded 2026-07-21. | Implementation |
| 16 | identicons | Channel identity guidance assumes a channel-id hash wherever an identicon appears, with the existing leading scope glyphs retained | Sidebar channel/session identicons replace their leading scope glyphs and hash session ids; the channel-detail hero hashes `channelType`, while task-agent badges hash their visible label | Intentional identity granularity and density: the hero is stable per channel type, rows are stable per session, badges agree with their label, and each row has one leading identity marker. Recorded 2026-07-21. | Implementation |
| 17 | health dashboard card headers | Uppercase eyebrow titles and right-aligned service status badges | Plain card titles with inline status badges | Intentional canonical-card simplification; the bespoke dashboard header treatment was removed rather than re-homed. Recorded 2026-07-21. | Implementation |
| 18 | shortcut hints | Settings and topbar rename affordances imply additional keyboard-shortcut hints | No new `kbd` hint is added where the product has no existing key-reference copy or implemented shortcut | Intentional scope narrowing: documenting a nonexistent shortcut would invent UX rather than clarify it. Recorded 2026-07-21. | Implementation |

## How to use this file

- When adding a new wireframe, check if its design matches the current implementation.
- If a deviation is intentional, add a row here with the resolution.
- Periodically review this list to see if deviations can be closed (wireframe updated or impl changed).
