# Environment Tools

- API: `http://localhost:3333` — DartClaw server for this fixture profile
- Auth: `Bearer devtoken0` (see `data/gateway_token`)
- Web UI: `http://localhost:3333` in browser
- Use `curl` + `jq` for API verification
- Use `git status` and `git diff` in the fixture repository to verify generated changes
