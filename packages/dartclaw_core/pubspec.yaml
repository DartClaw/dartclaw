name: dartclaw_core
description: Shared library for DartClaw — DB, bridge protocol, models, services.
publish_to: none
version: 0.2.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  sqlite3: ^3.1.6
  stream_channel: ^2.1.4
  uuid: ^4.5.2
  collection: ^1.19.0
  logging: ^1.3.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.29.0
  lints: ^6.0.0
