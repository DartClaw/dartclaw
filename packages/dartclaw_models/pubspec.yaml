name: dartclaw_models
description: Core data types for the DartClaw agent runtime — sessions, messages, memory chunks.
publish_to: none
version: 0.6.0

environment:
  sdk: ^3.11.0

resolution: workspace

dev_dependencies:
  test: ^1.29.0
  lints: ^6.0.0
