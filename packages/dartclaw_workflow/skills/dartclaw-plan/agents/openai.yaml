interface:
  display_name: "DartClaw Plan"
  short_description: "Create a PRD and story plan"
  default_prompt: "Use $dartclaw-plan to turn the provided requirements into a PRD-backed implementation plan."

policy:
  allow_implicit_invocation: true
