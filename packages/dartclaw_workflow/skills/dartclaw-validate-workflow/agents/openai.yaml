interface:
  display_name: "DartClaw Validate Workflow"
  short_description: "Validate workflow definitions and packaged assets"
  default_prompt: "Use $dartclaw-validate-workflow to validate the workflow definition or packaged assets at: "

policy:
  allow_implicit_invocation: true
