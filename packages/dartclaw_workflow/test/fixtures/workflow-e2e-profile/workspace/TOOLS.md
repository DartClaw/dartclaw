# Environment Tools

- API: `http://localhost:3333` when the profile is running
- Use `curl` + `jq` for API verification
- Use `git status` and `git diff` in the fixture repository to verify generated changes
