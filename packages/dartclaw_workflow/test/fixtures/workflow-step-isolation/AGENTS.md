# Workflow Step Isolation Fixture

This repository is the complete project root for workflow step isolation tests.

- Do not inspect parent or sibling repositories.
- Do not infer any external project document index.
- If local framework markers are absent, return `framework: none`.
- Treat this repository as intentionally minimal and self-contained.
