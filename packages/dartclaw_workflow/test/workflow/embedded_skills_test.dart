import 'package:dartclaw_workflow/dartclaw_workflow.dart';
import 'package:test/test.dart';

void main() {
  test('embeddedSkills stub decodes to an empty map', () {
    expect(embeddedSkills, isEmpty);
    expect(embeddedSkills.isEmpty, isTrue);
  });
}
